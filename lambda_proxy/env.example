# Lambda Proxy Configuration
# Copy this file to .env and fill in your values, then run:
#   export $(cat .env | xargs)

# Required: Your Bedrock AgentCore runtime ARN
# Get this from: agentcore status
AGENT_ARN=arn:aws:bedrock-agentcore:REGION:ACCOUNT_ID:runtime/YOUR_AGENT_ID

# Optional: AWS region (defaults to eu-west-1)
AWS_REGION=eu-west-1

# Optional: Lambda function name (defaults to idea-lattice-agent-proxy)
LAMBDA_FUNCTION_NAME=idea-lattice-agent-proxy

# Optional: API Gateway name (defaults to idea-lattice-agent-api)
API_GATEWAY_NAME=idea-lattice-agent-api

